#include <stdio.h>

int main() {
    int a = 5, b = 3;
    int sum, product;

    sum = a + b;
    product = a * b;

    printf("Sum: %d\n", sum);
    printf("Product: %d\n", product);

    return 0;
}
